# Candidate Task

请在本仓库中完成一个简化版 RAG 对话服务。你可以选择 Python 或 Java 其中一种语言实现。

## 笔试流程

1. 从面试官提供的分支开始开发。
2. 阅读本文件和项目结构。
3. 选择 `python/` 或 `java/` 其中一个目录完成实现。
4. 本地运行所选语言的测试。
5. 提交代码，并告知面试官你选择的语言和测试结果。

## 题目

项目中已经提供：

- 一个中文 `.docx` 文档；
- Python 和 Java 两套项目骨架；
- HTTP SSE 接口骨架；
- 本地模拟回答生成器；
- 单元测试。

你需要完成 RAG 的核心链路：

```text
读取 docx -> 生成文本片段 -> 关键词召回 -> 组装上下文 -> 返回模拟回答
```

项目不要求真实调用大模型。

## 需求

基础能力：

- 能从 `.docx` 中读取有效文本；
- 能把文档内容放入内存知识库；
- 能根据问题召回相关文本；
- 能基于召回内容生成上下文；
- 能通过已有接口返回结果。

变更能力：

- 召回结果需要有稳定排序；
- 重复内容需要处理；
- 无相关内容时需要返回兜底结果；
- 流式接口需要正常结束。

## 本地自测

Python：

```bash
cd python
python3 -m venv .venv
./.venv/bin/pip install -e ".[dev]"
./.venv/bin/pytest
```

Java：

```bash
cd java
mvn test
```

## 不允许做的事

- 不要修改测试来规避失败；
- 不要硬编码测试文档中的答案；
- 不要引入数据库、向量库或外部服务；
- 不要删除已有 HTTP 接口骨架；
- 不要提交密钥、令牌、虚拟环境、构建产物或缓存文件；
- 不要做与题目无关的大范围重构。

## 注意事项

- 优先保证实现简单、清晰、可测试；
- 只实现当前题目需要的能力；
- Java 实现需要兼容 Java 8；
- 提交信息请简要说明完成了什么。

